package WinMain

import PlayerHelper
import ExternalDefines
import IPlayerUnit

init
    Player(0).init_self(CreateUnitAtLoc(Player(0), ICE_MAGE, LOCATION_BORN, bj_UNIT_FACING), career.ice_mage)

    //Player(0).destr_self()